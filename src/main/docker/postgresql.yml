version: '2'
services:
  binapi-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Binapi/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Binapi
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
